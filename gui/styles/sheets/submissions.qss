/* ---------- FRAMES ---------- */
#rightFrame,
#submissionsHandlerFrame{
    background: transparent;
    border-radius:12px;
    margin:10px;
    padding:30px;
}

#rightFrame{
    padding:20px;
}

/* ---------- TITLE ---------- */
QLabel[text="Create a new submission"]{
    font-size:24px;
    font-weight:600;
    color:#FFFFFF;
    margin-bottom:20px;
}