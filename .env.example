# Copy this file to .env and fill in the values when marking.
# The submitted version of this internal should have the dotenv pre-included, but if not try and fill this out yourselves.

# OpenRouter / OpenAI compatible API settings
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: database paths (override defaults if needed)
# LOGIN_DB_PATH=./database/login.db
# LLM_DB_PATH=./database/LLM_testdatabase.db
